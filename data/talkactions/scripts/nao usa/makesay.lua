function onSay(cid, words, param)
	if param ~= "getPlayerVocation(Goku)" then
			doPlayerSendCancel(cid,"Udana Fuzja xD")

		else
			doPlayerSendCancel(cid,"You need to enter a player name and text")
		end
	end
